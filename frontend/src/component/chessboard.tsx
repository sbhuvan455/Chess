// We have to create the chess board here

import React from 'react'

function chessboard({
    board,
    setBoard
} : {
    board: string;
    setBoard: () => void;
}) {
  return (
    <div>
      
    </div>
  )
}

export default chessboard
